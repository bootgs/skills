---
type: llm
weight: 3
---
The response explains that `clasp push` updates HEAD only, that installed users run a versioned deployment, and that CI must also run `clasp deploy` against the existing deployment id for the fix to reach them. A response that says the process is fine, or blames caching, fails.
