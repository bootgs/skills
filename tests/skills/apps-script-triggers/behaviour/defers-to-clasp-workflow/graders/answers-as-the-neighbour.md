---
type: llm
weight: 2
---
The response explains that `clasp push` only updates HEAD and that installed users run a versioned deployment, so the fix needs a `clasp deploy`. A response about triggers, authorization, or event objects has answered a different question and fails.
